# express-react-redux-server-rendered-basic-starter-template
Starter template with express react redux server rendered and other basic features pre-installed

setup:
1. yarn 
2. yarn react
3. yarn dev

server starting on port 3000
